# SCSS 파일 구조

## 📁 디렉토리 구조

```
scss/
├── import/          # 기본 설정 및 유틸리티
│   ├── _variables.scss    # 변수 정의
│   ├── _mixin.scss       # Mixin 정의
│   ├── _typography.scss  # 타이포그래피
│   ├── _reset-css.scss   # CSS 리셋
│   └── ...
│
├── common/         # 공통 스타일
│   ├── _reset.scss        # 기본 리셋 및 전역 설정
│   ├── _typography.scss   # 타이포그래피 스타일
│   ├── _page-styles.scss  # 페이지별 공통 스타일
│   ├── _icons.scss        # 공통 아이콘 스타일
│   ├── _utilities.scss    # 유틸리티 클래스
│   ├── _modal.scss        # 모달 관련 스타일
│   ├── _animations.scss   # 공통 애니메이션 키프레임
│   ├── _responsive.scss   # 반응형 타이포그래피
│   └── _index.scss        # 공통 스타일 통합
│
├── layouts/        # 레이아웃 컴포넌트
│   ├── _header.scss      # 헤더
│   ├── _footer.scss      # 푸터
│   ├── _body.scss        # 바디
│   ├── _floating.scss    # 플로팅 메뉴
│   └── _index.scss       # 레이아웃 통합
│
├── components/     # 재사용 가능한 컴포넌트
│   ├── _buttons.scss     # 버튼
│   ├── _popup.scss       # 팝업
│   ├── _diagram.scss     # 다이어그램
│   ├── _intro.scss       # 인트로
│   ├── _layout-fix.scss  # 레이아웃 수정
│   ├── _mouse-mark.scss  # 마우스 마크
│   └── _index.scss       # 컴포넌트 통합
│
├── pages/          # 페이지별 스타일
│   ├── _main.scss        # 메인 페이지
│   ├── _value.scss       # 밸류 페이지
│   ├── _provided.scss    # 제공 페이지
│   ├── _primary.scss     # 주요 기능 페이지
│   ├── _analysis.scss    # 데이터분석 페이지
│   ├── _results.scss     # 결과 페이지
│   ├── _faq.scss         # FAQ 페이지
│   ├── _buy.scss         # 구매 페이지
│   └── _index.scss       # 페이지 통합
│
├── _base.scss      # 기본 설정 (mixin, 변수 forward)
├── _root.scss      # CSS 변수 정의
├── common.scss     # 공통 스타일 (메인 진입점)
└── style.scss      # 전체 스타일 (최종 진입점)
```

## 📝 파일 설명

### Import 폴더
- **변수, Mixin, 리셋 등 기본 설정 파일들**
- 다른 파일에서 `@use "../import/_mixin" as *;` 형태로 사용

### Common 폴더
- **공통으로 사용되는 스타일들**
- Reset, Typography, Page Styles, Icons, Utilities, Modal, Animations, Responsive
- 모든 페이지에서 공통으로 사용되는 스타일

### Layouts 폴더
- **전역 레이아웃 컴포넌트** (Header, Footer 등)
- 페이지 전체에 영향을 주는 구조적 요소

### Components 폴더
- **재사용 가능한 UI 컴포넌트**
- 여러 페이지에서 공통으로 사용되는 요소

### Pages 폴더
- **페이지별 고유 스타일**
- 특정 페이지에만 적용되는 스타일
- 각 페이지는 하위 폴더로 모듈화되어 있음 (예: `analysis/`, `primary/`, `results/`)

## 🔧 사용 방법

### Mixin 사용 (각 파일에서 직접 import)
```scss
@use "../import/_mixin" as *;

.my-class {
  @include flex(center, center);
}
```

### 변수 사용
```scss
@use "../import/_variables" as *;

.my-class {
  color: var(--color-green);
}
```

### 경로 규칙
- **layouts 폴더에서**: `@use "../import/_mixin" as *;`
- **components 폴더에서**: `@use "../import/_mixin" as *;`
- **pages 폴더에서**: `@use "../../import/_mixin" as *;`
- **scss 루트에서**: `@use "import/_mixin" as *;`

### Base 사용
필요한 경우 `@use "base" as *;`로 사용할 수 있지만,
일반적으로는 각 파일에서 직접 필요한 것만 import하는 것을 권장합니다.

## 📦 로드 순서

1. **style.scss** (최종 진입점)
   - `_root.scss` (CSS 변수)
   - `common.scss` (공통 스타일)
   - `pages/_index.scss` (페이지별 스타일)

2. **common.scss**
   - `import/` (기본 설정)
   - `layouts/_index.scss` (레이아웃)
   - `components/_index.scss` (컴포넌트)
   - `common/_index.scss` (공통 스타일)

3. **pages/_index.scss**
   - 각 페이지별 스타일 forward

## 🎯 모듈화 원칙

- 각 페이지는 하위 폴더로 모듈화
- 공통 스타일은 `common/` 폴더에 분리
- 페이지 전용 애니메이션은 각 페이지의 `_keyframes.scss`에 정의
- 공통 애니메이션은 `common/_animations.scss`에 정의
