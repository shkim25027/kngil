# SCSS 파일 구조

## 📁 디렉토리 구조

```
scss/
├── import/          # 기본 설정 및 유틸리티
│   ├── _variables.scss    # 변수 정의
│   ├── _mixin.scss       # Mixin 정의
│   ├── _typography.scss  # 타이포그래피
│   ├── _reset-css.scss   # CSS 리셋
│   └── ...
│
├── layouts/        # 레이아웃 컴포넌트
│   ├── _header.scss      # 헤더
│   ├── _footer.scss      # 푸터
│   └── _index.scss       # 레이아웃 통합
│
├── components/     # 재사용 가능한 컴포넌트
│   ├── _buttons.scss     # 버튼
│   ├── _popup.scss       # 팝업
│   ├── _floating.scss    # 플로팅 메뉴
│   ├── _diagram.scss     # 다이어그램
│   └── _index.scss       # 컴포넌트 통합
│
├── pages/          # 페이지별 스타일
│   ├── _main.scss        # 메인 페이지
│   ├── _value.scss       # 밸류 페이지
│   ├── _interface.scss   # 인터페이스 페이지
│   └── _index.scss       # 페이지 통합
│
├── _base.scss      # 기본 설정 (mixin, 변수 forward)
├── _root.scss      # CSS 변수 정의
├── _animations.scss # 애니메이션
├── common.scss     # 공통 스타일 (메인 진입점)
└── style.scss      # 페이지 스타일 (메인 진입점)
```

## 📝 파일 설명

### Import 폴더
- **변수, Mixin, 리셋 등 기본 설정 파일들**
- 다른 파일에서 `@use "../import/_mixin" as *;` 형태로 사용

### Layouts 폴더
- **전역 레이아웃 컴포넌트** (Header, Footer 등)
- 페이지 전체에 영향을 주는 구조적 요소

### Components 폴더
- **재사용 가능한 UI 컴포넌트**
- 여러 페이지에서 공통으로 사용되는 요소

### Pages 폴더
- **페이지별 고유 스타일**
- 특정 페이지에만 적용되는 스타일

## 🔧 사용 방법

### Mixin 사용 (각 파일에서 직접 import)
```scss
// 각 파일 상단에 필요한 mixin import
@use "../import/_mixin" as *;

.my-component {
  @include flex(center, space-between);
  @include mq(tablet-small, max) {
    // 모바일 스타일
  }
}
```

### 변수 사용
```scss
@use "../import/_variables" as *;

.my-element {
  color: var(--color-primary);
}
```

### 경로 규칙
- **layouts 폴더에서**: `@use "../import/_mixin" as *;`
- **components 폴더에서**: `@use "../import/_mixin" as *;`
- **pages 폴더에서**: `@use "../import/_mixin" as *;`
- **scss 루트에서**: `@use "import/_mixin" as *;`

### _base.scss 사용 (선택사항)
`_base.scss`는 mixin과 px-convert를 forward하는 파일입니다.
필요한 경우 `@use "base" as *;`로 사용할 수 있지만,
**권장사항: 각 파일에서 필요한 것만 직접 import**

## 📌 규칙

1. **파일명은 언더스코어(_)로 시작** (SCSS partial 파일)
2. **각 폴더의 _index.scss에서 통합 관리**
3. **Mixin이 필요한 파일에만 import 추가**
4. **페이지별 스타일은 pages 폴더에만 작성**
