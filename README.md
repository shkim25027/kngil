1. Gulp 설치
   
   1-1 .Gulp CLI 전역 설치(권장)
   npm install --global gulp-cli

   1-2. Gulp 프로젝트 설치(로컬)
   npm install --save-dev gulp

2. Gulp 실행

   터미널에서 프로젝트 루트에서: gulp

3. Gulp dist 경로 수정은 gulpfile.js 파일
